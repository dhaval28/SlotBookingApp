import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
    loginBox: {
        height: 200,
        width: 200,
        padding: 20,
        borderWidth: 3,
        marginLeft: 100,
        marginBottom: 50,
        backgroundColor: 'lightblue',
    }
});